package com.saga.concessionaria.controller;

import org.springframework.http.ResponseEntity;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;

@Controller("/api/feedback")
public class FeedbackController {
	
	/*
	 * @PostMapping("/send") public ResponseEntity<String> sendFeedback(@RequestBody
	 * FeedbackRequest feedbackRequest) { // Lógica para enviar feedback via e-mail
	 * ou outro canal return ResponseEntity.ok("Pesquisa enviada com sucesso."); }
	 */
}
